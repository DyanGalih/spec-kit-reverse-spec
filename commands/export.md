# Command: /speckit.reverse-spec.export

Export the generated pipeline reports and final specifications.

## Behavior
1. Compile all validation and alignment data.
2. Generate `.reverse-spec/export-report.md`.
3. Generate `.reverse-spec/pipeline-report.md`.
4. Finalize the `specs/` directory for implementation.
